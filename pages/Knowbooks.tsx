import { observer } from "mobx-react";
import KnowbooksText from "../src/components/KnowbooksText";

const Knowbooks: React.FunctionComponent = (props) => {
  return <KnowbooksText />;
};

export default observer(Knowbooks);
