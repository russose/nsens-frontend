import { Text, Badge } from "gestalt";

//Badge missing
export const JsBadge = (props) => {
  return <Badge {...props} />;
};

//Weight attribute missing
export const JsText = (props) => {
  return <Text {...props} />;
};
