import { SearchField } from "gestalt";

//Badge missing
// export const JsBadge = (props) => {
//   return <Badge {...props} />;
// };

//onKeyDown Missing
export const JsSearchField = (props) => {
  return <SearchField {...props} />;
};
