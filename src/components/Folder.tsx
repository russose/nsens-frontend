import { Box } from "gestalt";

const Folder: React.FunctionComponent = (props) => {
  return (
    <div>
      <Box color="gray" padding={0}>
        <div> Folder</div>
      </Box>
    </div>
  );
};

export default Folder;
